let minutes = 0;
let seconds = 0;

minutes = 45;
seconds = 6;

if (seconds === 0) {
  console.log(`${minutes} minutes`);
} else {
  console.log(`${minutes} minutes ${seconds} seconds`);
}

const str = "iterable element";
